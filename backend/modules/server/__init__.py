"""
Server Module - ML Predictions & Alert System

Handles active model management, ML predictions, alert evaluation,
and n8n webhook notifications. Uses direct Python imports from the
training module instead of HTTP calls.
"""
